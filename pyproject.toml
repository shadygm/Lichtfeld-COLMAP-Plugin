[project]
name = "colmap_plugin"
version = "0.1.0"
requires-python = ">=3.12"
description="COLMAP reconstruction plugin"

dependencies = [
  "pycolmap",
]

[tool.lichtfeld]
hot_reload = true
plugin_api = ">=1,<2"
lichtfeld_version = ">=0.5.0"
required_features = []